{% extends 'base.html.twig' %}

{% block title %}Fruit{% endblock %}

{% block body %}
    <h1>Fruit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fruit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fruit.name }}</td>
            </tr>
            <tr>
                <th>Family</th>
                <td>{{ fruit.family }}</td>
            </tr>
            <tr>
                <th>Genus</th>
                <td>{{ fruit.genus }}</td>
            </tr>
            <tr>
                <th>Fruit_order</th>
                <td>{{ fruit.fruitOrder }}</td>
            </tr>
            <tr>
                <th>Fruity_vice_id</th>
                <td>{{ fruit.fruityViceId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fruit_index') }}">back to list</a>

    <a href="{{ path('app_fruit_edit', {'id': fruit.id}) }}">edit</a>

    {{ include('fruit/_delete_form.html.twig') }}
{% endblock %}
