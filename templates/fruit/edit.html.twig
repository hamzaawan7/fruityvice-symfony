{% extends 'base.html.twig' %}

{% block title %}Edit Fruit{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Edit Fruit</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_fruit_index') }}">
                    View All Fruits
                </a>
                <a class="btn btn-outline-warning float-right" href="{{ path('app_fruit_index') }}">
                    Back To Fruits
                </a>
                <div class="card-body">
                    {% include 'fruit/_form.html.twig' with {'button_label': 'Update Project', 'button_color': 'btn-outline-success'} %}
                    <br/>
                    {{ include('fruit/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
